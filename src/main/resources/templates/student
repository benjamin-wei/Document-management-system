<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml"
      xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Sample</title>
</head>
<body>
    <h1 th:inline="text">能力规范文稿管理系统</h1>
    <h2> 入会申请<h2>
<form action="action_page.php">
姓名:<br>
<input type="text" name="firstname" value="Mickey">
<br>
性别:<br>
<input type="text" name="lastname" value="Mouse">
<br><br>
<input type="submit" value="Submit">
</form>
<p>已有账户？<a href = "http://localhost:8080/Login/getAllStudent" >点此</a>登陆</p>
<!--
    <table>
        <!-- 名称 -->
        <thead>
             <tr>
                 <th >id</th>
                 <th >学号</th>
                 <th >姓名</th>
                 <th >成绩</th>
             </tr>

         </thead>
         <!-- 动态插入内容 -->
         <tbody>
             <tr th:each="s,iterStat : ${students}" th:class="${iterStat.odd}? 'odd'">
                     <td th:text=" ${s.getId()}"></td>
                 　　<td th:text=" ${s.getNo()}"></td>
                 　　<td th:text=" ${s.getName()}"></td>
                 　　<td th:text=" ${s.getScore()}"></td>
                    </td>
             </tr>
         </tbody>
    </table>
    -->
</body>
</html>